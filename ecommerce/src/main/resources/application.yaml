spring:
  application:
    name: kiet-ecommerce

  datasource:
    url: "jdbc:mysql://localhost:3306/ecommerce"
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
  servlet:
    context-path: /ecommerce